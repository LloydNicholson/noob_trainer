player_move();
move_movement_entity(false);

//// Not in use at the moment
//// Warp from one side of room to another
//if x < x_min && place_free(x-1, y) {
//	x = x_max;	
//} else if x > x_max && place_free(x-1, y) {
//	x = x_min;	
//} 

